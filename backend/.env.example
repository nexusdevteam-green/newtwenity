SUPABASE_URL=https://TU-PROYECTO.supabase.co
SUPABASE_ANON_KEY=tu-anon-key-aqui
PORT=4000

# URL del frontend usada para construir el enlace de "restablecer contraseña".
FRONTEND_URL=http://localhost:5173

# Orígenes autorizados por CORS, separados por comas. Si no se define, se usa
# FRONTEND_URL como único origen permitido. En producción incluye tanto la URL
# del frontend desplegado como tu localhost si quieres seguir probando en local
# contra el backend en Render.
# ALLOWED_ORIGINS=https://tuenties.onrender.com,http://localhost:5173
